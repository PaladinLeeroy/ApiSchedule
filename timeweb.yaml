name: schedule-management-system
runtime: python
version: "3.9"

build:
  commands:
    - pip install -r requirements.txt

run:
  command: python timeweb_app.py
  port: 8000

env:
  - name: SECRET_KEY
    value: "C_K7C2mrtbXjYCvOX2ZEF_agSayGl98-LSD0eiMrluI"
  - name: ALGORITHM
    value: "HS256"
  - name: ACCESS_TOKEN_EXPIRE_MINUTES
    value: "30"
  - name: ENVIRONMENT
    value: "production"
  - name: HOST
    value: "0.0.0.0"
  - name: PORT
    value: "8000"
  - name: DEBUG
    value: "False"

# Для SQLite (временное решение)
# DATABASE_URL будет настроен через панель управления

# Для PostgreSQL (рекомендуется для продакшена)
# DATABASE_URL: postgresql://username:password@host:port/database 